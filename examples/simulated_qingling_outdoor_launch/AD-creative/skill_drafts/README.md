# Skill Drafts

Project-local reusable workflow drafts live here.

Do not install into `~/.codex/skills` without explicit user approval.

Each draft should contain:

```text
SKILL.md
evidence.md
install_request.md
```
