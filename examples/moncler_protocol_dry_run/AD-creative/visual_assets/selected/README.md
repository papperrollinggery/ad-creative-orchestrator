# Selected

QA-passed internal visual assets.

Rules:
- Selected does not automatically mean client-visible.
- Client visibility still requires gate approval.
- Keep source prompt or source reference traceable in `asset_manifest.csv`.
